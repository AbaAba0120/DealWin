# 成交喜报海报生成器 — 使用与部署说明

## 这是什么

`poster-app/` 是一个纯静态网页：在表单里填写成交数据、上传照片，实时预览并导出 2480×3508(A4 300dpi)高清 PNG 海报。所有数据只在使用者自己的浏览器里处理，不上传任何服务器。

## 使用说明

- **文字内容**：左侧表单按分区填写。标签和单位("建成年代：""（套）""元/平米"等)全部固定，只需填数值;含"/"的字段会拆成多个输入框(如 总户数/楼栋数 填两个数),海报自动按原格式拼回。文字过长会自动缩小字号防止溢出。
- **自动计算**：谈判溢价 = 成交价 − 客户出价，谈判溢价率 = 溢价 ÷ 客户出价，填完成交价和客户出价后自动算出并实时显示。客户出价默认为成交价的 92%，可在 85%–97% 之间修改（下方有比例滑条，可拖动调节），超出范围自动夹取。委托-后的「拍摄人数 / 带看量 / 出价量 / 谈判量」会自动填一个随机值（带看量为门店量的 30%–120%，出价量为带看量的 5%–15%），每个字段下方都有滑条可拖动调节，也可直接在输入框手改。
- **成交日期**：分年/月/日三个框填写。
- **装修折价**：房屋信息的「装修」行可选择"是否有折价",选"是"会多出一个折价输入框，海报显示为"装修/折价：精装/30万"。
- **成交照片（右上大图）**：在「成交照片」分区上传。上传后照片自动填入海报右上区域。
- **上传方式（所有照片通用）**：三种方式——①点击选择本地文件；②点击分区底部的激活条后 Ctrl+V 粘贴剪贴板图片；③从微信、飞书、文件管理器等直接把图片拖到表单分区上，或拖到预览区对应的照片位置。
- **贴纸·马赛克（所有照片通用）**：在表单最底部的独立分区，包含内置贴纸、emoji、自定义上传贴纸和马赛克画笔。点击贴纸后自动落到海报画布中央，可**拖动**移动位置、**右下角蓝色方块**缩放、**左下角绿色手柄**旋转、**红色 ×** 删除——用于遮挡人脸或装饰。上传自定义贴纸（可多选）后会同时加入贴纸库并放一个实例到画布中央。
- **马赛克画笔**：点击「马赛克画笔」进入涂抹模式，预览区变为十字光标，选择笔刷大小（小/中/大）后在照片上涂抹。涂抹区域以半透明白色显示，导出时才是真正马赛克效果（像素化）。提供「撤销上一笔」和「清除全部」。马赛克只影响照片，贴纸和文字保持清晰。
- **项目照片（下方 4 个分类）**：每个分类最多 12 张，可一次选多张，自动网格排版；也可用「排版」下拉切换预设网格（满幅/左右/上下/上一下二/田字/六宫格/九宫格/十二宫格）；每个照片格还可以直接**拖动位置**、**右下角缩放**做自由微调。
- **下载**：点「下载高清 PNG」导出最终海报。「恢复默认内容」可一键还原示例文字并清空照片贴纸。

## 文件结构

```
poster-app/
├── index.html      页面入口
├── render.js       渲染、表单与交互逻辑
├── layout.js       从 PSD 提取的版面数据(文字位置/字号/颜色)
├── assets.js       背景图与贴纸(base64 内嵌)
├── layout.json     版面数据(调试用)
├── assets/         背景图、贴纸原图(调试用)
└── fonts/          思源黑体 Heavy/Medium/Light(海报字体)
```

## 方式一：直接使用(最简单)

把整个 `poster-app` 文件夹发给同事,双击 `index.html` 即可使用(推荐 Chrome / Edge)。
注意:必须整个文件夹一起发,`fonts/` 等文件不能缺。

## 方式二：内网/静态托管

把 `poster-app` 整个目录放到任意静态文件服务器(公司内网 Nginx、IIS、NAS 的 Web 服务、或公网静态托管如 Vercel/Gitee Pages 等),同事通过网址访问。无需任何后端。

## 方式三：部署到飞书应用

前提:网页已通过方式二有一个可访问的 URL(飞书客户端内嵌浏览器打开该网址,同事手机/电脑能访问该地址即可;HTTPS 最佳)。

1. 用企业管理员账号登录 [飞书开放平台](https://open.feishu.cn/),进入「开发者后台」
2. 「创建自建应用」,填写名称(如"成交喜报生成器")和图标
3. 左侧「添加应用能力」→ 添加「网页应用」
4. 在「网页应用」配置里填写:
   - 桌面端主页:你的页面 URL(如 `https://内网地址/poster-app/index.html`)
   - 移动端主页:同上
5. 「版本管理与发布」→ 创建版本并提交发布(自建应用一般管理员审核通过即可)
6. 同事在飞书「工作台」找到该应用,点开即用

> 提示:如果只是团队内部用,方式一(发文件夹)或方式二(内网网址)已经够用,飞书应用只是多一个统一入口。

## 关于字体

`fonts/` 内置开源免费的思源黑体(Heavy/Medium/Light 对应原海报的特黑/中黑/细黑),无版权问题,可随意分发部署。
如需与 PSD 完全一致,可把方正兰亭黑字体文件放进 `fonts/` 并在 `render.js` 的 `loadFonts()` 里替换字体路径。
